export const SolaceLogo = (props: React.HTMLAttributes<HTMLDivElement>) => {
  return (
    <div {...props} className={`flex items-center ${props.className}`}>
      <span className="font-serif text-xl font-bold tracking-wider uppercase">
        Carmela Art
      </span>
    </div>
  )
}
